<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pantallas"

        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.principal.Principal"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pantallas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.fotoUsuario.FotoUsuario" android:exported="false" />
        <activity android:name=".ui.login.Login" android:exported="true" />
        <activity android:name=".ui.registro.Registrar" android:exported="true" />
        <activity android:name=".ui.favoritos.Favoritos" android:exported="true" />
        <activity android:name=".ui.perfil.Perfil" android:exported="true" />
        <activity android:name=".ui.editarPerfil.EditarPerfil" android:exported="true" />
        <activity android:name=".ui.biblioteca.Biblioteca" android:exported="true" />

    </application>
</manifest>